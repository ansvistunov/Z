
package calc.objects;
import dbi.*;

public class DATASTORE implements class_constructor {
    //аргумент - alias
    public Object constructor(Object arg) throws Exception {
        dbi.DATASTORE ds = new dbi.DATASTORE();
        if (arg instanceof String) {
            
        }        
        return ds;
    }
}
